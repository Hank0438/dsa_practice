#include <stdio.h>

int main(){
	s = stack;
	while(postfix尚未由左而右掃完){
		x = 下一個字;
		if(x是operand){ push(s,x);}
		else{
			//pop適當數目的operand
			//執行計算
			//push結果 
		} 
	} 
	printf(pop(s)); //此時stack只有一個element,即為運算結果 
}
