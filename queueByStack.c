#include <stdio.h>

void Enqueue(QUEUE *q, int y){
	push(s,item);
}

int Dequeue(QUEUE *q){
	if(isEmpty(t)){
		if(isEmpty(s){
			printf("Q is empty\n"); 
		}else{
			while(!isEmpty(s)){
				x = pop(s);
				push(s,x);
			}
		}
	}
	item = pop(t);
	return item;
}

void main(){
	stack s,t;
	QUEUE q;
}
